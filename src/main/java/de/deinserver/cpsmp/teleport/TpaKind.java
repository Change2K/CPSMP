package de.deinserver.cpsmp.teleport;

public enum TpaKind {
    /** Sender teleports to target after target accepts. */
    TPA,
    /** Target teleports to sender after target accepts. */
    HERE
}
